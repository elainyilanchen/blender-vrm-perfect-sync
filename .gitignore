# Downloaded portable Blender + add-on (regenerated by the setup wizard)
runtime/

# Processing outputs and local working files
out/
*.bak

# Local user config (contains machine-specific paths)
gui_config.json

# Python cache
__pycache__/
*.pyc

# Editor / agent
.claude/

# VRM models: keep personal models out of the repo.
# The curated sample donors under samples/ are the only tracked .vrm files.
*.vrm
!samples/*.vrm

# Personal working notes (kept local, not published)
VRoid完美同步方案.md
